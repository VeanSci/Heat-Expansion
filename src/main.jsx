import React from "react";
import ReactDOM from "react-dom/client";
import ThermalExpansionApp from "./ThermalExpansion.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ThermalExpansionApp />
  </React.StrictMode>
);
